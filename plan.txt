
		    I.saveMap = function (name) {
			    var myString = JSON.stringify(I.tiles); //!?!
				localStorage.setItem(name, myString);
			}
		    loadMap = function (name) {
				poop=localStorage.getItem(name);
				var myObject = JSON.parse(poop);
				return myObject;
			}
			
